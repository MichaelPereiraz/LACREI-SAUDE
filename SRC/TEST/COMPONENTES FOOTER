import React from 'react';
import { Link } from 'react-router-dom';
import { Heart, Mail, Phone, MapPin, Facebook, Instagram, Twitter, Linkedin } from 'lucide-react';
import { Button } from '@/components/ui/button';
import lacreiLogo from '@/assets/lacrei-logo.png';

const Footer = () => {
  const currentYear = new Date().getFullYear();

  const socialLinks = [
    { name: 'Facebook', icon: Facebook, href: '#', label: 'Siga-nos no Facebook' },
    { name: 'Instagram', icon: Instagram, href: '#', label: 'Siga-nos no Instagram' },
    { name: 'Twitter', icon: Twitter, href: '#', label: 'Siga-nos no Twitter' },
    { name: 'LinkedIn', icon: Linkedin, href: '#', label: 'Conecte-se no LinkedIn' },
  ];

  const quickLinks = [
    { name: 'Início', href: '/' },
    { name: 'Sobre Nós', href: '/sobre' },
    { name: 'Serviços', href: '/servicos' },
    { name: 'Contato', href: '/contato' },
  ];

  const legalLinks = [
    { name: 'Política de Privacidade', href: '/privacidade' },
    { name: 'Termos de Uso', href: '/termos' },
    { name: 'Código de Ética', href: '/etica' },
    { name: 'Acessibilidade', href: '/acessibilidade' },
  ];

  return (
    <footer 
      className="bg-gradient-to-br from-primary/5 to-secondary/5 border-t border-border"
      role="contentinfo"
      aria-label="Rodapé da página"
    >
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
        {/* Main Footer Content */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
          {/* Brand Section */}
          <div className="lg:col-span-2">
            <Link 
              to="/" 
              className="flex items-center space-x-3 group focus:outline-none focus:ring-2 focus:ring-primary rounded-lg p-1 w-fit"
              aria-label="Lacrei Saúde - Página inicial"
            >
              <img 
                src={lacreiLogo} 
                alt="Lacrei Saúde"
                className="h-12 w-12 transition-transform group-hover:scale-105"
                loading="lazy"
              />
              <div className="flex flex-col">
                <span className="text-2xl font-heading font-bold text-foreground group-hover:text-primary transition-colors">
                  Lacrei Saúde
                </span>
                <span className="text-sm text-muted-foreground">
                  Cuidar com respeito e inclusão
                </span>
              </div>
            </Link>
            
            <p className="mt-4 text-muted-foreground leading-relaxed max-w-md">
              Somos uma plataforma de saúde digital inclusiva, conectando a comunidade LGBTQIAPN+ 
              a profissionais capacitados e serviços respeitosos. Cada pessoa merece cuidado com dignidade.
            </p>

            {/* Contact Info */}
            <div className="mt-6 space-y-3">
              <div className="flex items-center space-x-3 text-sm text-muted-foreground">
                <Mail className="h-4 w-4 text-primary" aria-hidden="true" />
                <a 
                  href="mailto:contato@lacreisaude.com.br" 
                  className="hover:text-primary transition-colors focus:outline-none focus:underline"
                  aria-label="Enviar email para contato@lacreisaude.com.br"
                >
                  contato@lacreisaude.com.br
                </a>
              </div>
              <div className="flex items-center space-x-3 text-sm text-muted-foreground">
                <Phone className="h-4 w-4 text-primary" aria-hidden="true" />
                <a 
                  href="tel:+5511999999999" 
                  className="hover:text-primary transition-colors focus:outline-none focus:underline"
                  aria-label="Ligar para (11) 99999-9999"
                >
                  (11) 99999-9999
                </a>
              </div>
              <div className="flex items-center space-x-3 text-sm text-muted-foreground">
                <MapPin className="h-4 w-4 text-primary flex-shrink-0" aria-hidden="true" />
                <span>São Paulo, SP - Brasil</span>
              </div>
            </div>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="text-lg font-heading font-semibold text-foreground mb-4">
              Links Rápidos
            </h3>
            <nav role="navigation" aria-label="Links rápidos do rodapé">
              <ul className="space-y-3">
                {quickLinks.map((link) => (
                  <li key={link.name}>
                    <Link
                      to={link.href}
                      className="text-muted-foreground hover:text-primary transition-colors text-sm focus:outline-none focus:underline"
                    >
                      {link.name}
                    </Link>
                  </li>
                ))}
              </ul>
            </nav>
          </div>

          {/* Legal Links */}
          <div>
            <h3 className="text-lg font-heading font-semibold text-foreground mb-4">
              Informações Legais
            </h3>
            <nav role="navigation" aria-label="Informações legais">
              <ul className="space-y-3">
                {legalLinks.map((link) => (
                  <li key={link.name}>
                    <Link
                      to={link.href}
                      className="text-muted-foreground hover:text-primary transition-colors text-sm focus:outline-none focus:underline"
                    >
                      {link.name}
                    </Link>
                  </li>
                ))}
              </ul>
            </nav>
          </div>
        </div>

        {/* Newsletter Subscription */}
        <div className="mt-12 pt-8 border-t border-border">
          <div className="max-w-md mx-auto lg:mx-0">
            <h3 className="text-lg font-heading font-semibold text-foreground mb-4 flex items-center">
              <Heart className="h-5 w-5 text-primary mr-2" aria-hidden="true" />
              Fique por dentro das novidades
            </h3>
            <p className="text-sm text-muted-foreground mb-4">
              Receba atualizações sobre saúde inclusiva e novos serviços da Lacrei Saúde.
            </p>
            <form className="flex flex-col sm:flex-row gap-3" role="form" aria-label="Formulário de newsletter">
              <input
                type="email"
                placeholder="Seu melhor email"
                className="flex-1 px-4 py-3 rounded-lg border border-input bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent"
                aria-label="Digite seu email para receber newsletter"
                required
              />
              <Button 
                type="submit" 
                variant="secondary"
                className="sm:w-auto"
                aria-label="Inscrever-se na newsletter"
              >
                Inscrever-se
              </Button>
            </form>
          </div>
        </div>

        {/* Social Media & Copyright */}
        <div className="mt-12 pt-8 border-t border-border flex flex-col lg:flex-row justify-between items-center gap-6">
          {/* Social Media */}
          <div className="flex items-center space-x-4">
            <span className="text-sm text-muted-foreground">Siga-nos:</span>
            <div className="flex space-x-2">
              {socialLinks.map((social) => {
                const Icon = social.icon;
                return (
                  <a
                    key={social.name}
                    href={social.href}
                    className="p-2 rounded-lg text-muted-foreground hover:text-primary hover:bg-accent/10 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-primary"
                    aria-label={social.label}
                  >
                    <Icon className="h-5 w-5" aria-hidden="true" />
                  </a>
                );
              })}
            </div>
          </div>

          {/* Copyright */}
          <div className="text-center lg:text-right">
            <p className="text-sm text-muted-foreground">
              © {currentYear} Lacrei Saúde. Todos os direitos reservados.
            </p>
            <p className="text-xs text-muted-foreground mt-1">
              Feito com <Heart className="inline h-3 w-3 text-primary mx-1" aria-hidden="true" /> 
              para a comunidade LGBTQIAPN+
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
